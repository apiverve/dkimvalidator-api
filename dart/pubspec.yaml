name: apiverve_dkimvalidator
description: DKIM Validator checks the DomainKeys Identified Mail (DKIM) DNS records for a domain to verify that they are present and correctly formatted.
version: 1.1.14
homepage: https://apiverve.com/marketplace/dkimvalidator?utm_source=dart&utm_medium=homepage
repository: https://github.com/apiverve/dkimvalidator-api/tree/main/dart

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0
  http_parser: ^4.0.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
